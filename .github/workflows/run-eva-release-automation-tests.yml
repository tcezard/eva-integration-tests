name: Run Tests from EVA Release Automation

on:
  repository_dispatch:
    types: [trigger-eva-release-autoamtion-tests]

jobs:
  build:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: [3.11]
    steps:
    - uses: actions/checkout@v2
    - name: Set up Python ${{ matrix.python-version }}
      uses: actions/setup-python@v2
      with:
        python-version: ${{ matrix.python-version }}
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt
    - name: Lint with flake8
      run: |
        # stop the build if there are Python syntax errors or undefined names
        flake8 . --count --select=E9,F63,F7,F82 --show-source --statistics
        # exit-zero treats all errors as warnings. The GitHub editor is 127 chars wide
        flake8 . --count --exit-zero --max-complexity=10 --max-line-length=127 --statistics
    - name: Test with pytest
      run: |
        echo $SOURCE_GITHUB_REPOSITORY
        echo $SOURCE_GITHUB_REF
        echo $SOURCE_GITHUB_SHA
        PYTHONPATH=. pytest tests/components/eva_release_automation
      env:
          SOURCE_GITHUB_REPOSITORY:  ${{ github.event.client_payload.repository }}
          SOURCE_GITHUB_REF:  ${{ github.event.client_payload.ref }}
          SOURCE_GITHUB_SHA:  ${{ github.event.client_payload.sha }}
